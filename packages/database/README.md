# Database

This package stores SQL migrations for the first production database baseline.

Phase 1 targets PostgreSQL. Local migration execution will be wired when DEV-002
adds real authentication, organization, role, and user tables to the running API.
