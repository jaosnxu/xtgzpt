# DEV-017 页面状态和响应式生产化收口

## 状态

- 状态：代码实现完成；外部 verifier gate 和 Browser DOM 验收通过
- 分支：当前 Loop 工作树
- 前置：`DEV-016` AI Framework / AI Run 生产化
- Loop 任务：`TASK-XTGZPT-DEV-017-CONTINUATION-CODEX-1`

## 范围

- Web 前端 1440 / 1280 / 960 办公宽度响应式约束
- 左侧一级导航在 960 宽度仍保留为侧栏，不新增顶级菜单
- Dashboard、Workbench、Projects、Tasks、Chat、Knowledge、Contracts、Approvals、System Settings、Login 的状态面提示补强
- 长文本、俄文长度文本、证据描述、状态 badge、toolbar、卡片、列表行的换行、截断和稳定尺寸
- AI Framework / AI Runs 继续留在 System Settings 和来源对象相关界面内

## 不做

- 不新增业务模块、一级菜单、财务、采购、库存、ERP、外部 agent、外部向量库、外部通知、移动 App、部署、secrets 或生产写入
- 不改后端业务状态机、权限模型或数据库 schema
- 不允许 AI 审批、驳回、退回、转交、加签、发布知识、创建正式任务、签署、付款或确认执行

## 当前执行记录

| 时间 | 动作 | 结果 |
| --- | --- | --- |
| 2026-06-20 | 读取 `App.tsx`、`styles.css`、页面状态响应式标准和 DEV-016 日志 | 已完成 |
| 2026-06-20 | 收紧应用宽度、侧栏、topbar、metric、panel、row、badge、state notice 的响应式约束 | 已完成 |
| 2026-06-20 | 将 960 宽度从顶部导航回退改为保留左侧导航，860 以下才进入窄屏堆叠 | 已完成 |
| 2026-06-20 | 为 Workbench、Chat、Knowledge、Contracts、Approvals、Login 补入已有数据驱动的状态提示 | 已完成 |
| 2026-06-20 | 禁用归档会话继续发送消息或生成 AI 草稿 | 已完成 |
| 2026-06-20 | 写入 DEV-017/AUDIT-017 和 runtime memory | 已完成 |
| 2026-06-20 | 外部 verifier 补跑 Node/npm gate、audit、smoke 和 Browser DOM 验收 | 已通过 |
| 2026-06-20 | 根据真实 verifier 结果修复 DEV-017/AUDIT-017 和 runtime memory 验证记录 | 已完成 |

## 验证结果

已通过：

- `git diff --check`：通过
- `rg` 扫描 `AI Center` / 顶级 AI 菜单相关字符串：通过，无匹配
- 旧 DEV-016 顶栏文案扫描：通过，主顶栏已更新为 DEV-017
- `npm run lint`：通过
- `npm run typecheck`：通过
- `npm run test`：通过，14 个 test files / 56 个 tests
- `npm run build`：通过
- `npm run smoke:api`：通过，29 checks
- `npm audit --audit-level=low`：通过，0 vulnerabilities
- Browser DOM 验收：通过，覆盖 Dashboard、Workbench、Projects、Tasks、Chat、Knowledge、Contracts、Approvals、System Settings、Login/menu flow

Browser 验收说明：

- 已在当前 in-app Browser 可用的 1280 和 960 viewports 完成 DOM 验证。
- Dashboard、Workbench、Projects、Tasks、Chat、Knowledge、Contracts、Approvals、System Settings、Login/menu flow 均无 horizontal overflow，且无 out-of-bounds elements。
- 请求的 1440 viewport 被当前 in-app Browser 约束为 1280。这是工具限制，不是产品失败；DEV-017 CSS 在 1440 office layout 内使用 1240 content max，1280 DOM 验收已覆盖该内容宽度约束。
- Browser CDP screenshot capture 超时；DOM 验证已通过。

## 当前边界

已完成：

- 响应式和状态面生产化收口集中在 `apps/web/src/ui/App.tsx` 与 `apps/web/src/styles.css`
- 未新增顶级菜单
- AI Framework / AI Runs 保持在既有 System Settings 和来源对象相关表面内
- AI human-boundary 规则未放宽

限制记录：

- 当前 in-app Browser 无法提供请求的 1440 viewport，实际可用最大宽度为 1280。
- Browser CDP screenshot capture 超时，但 DOM 验收已完成并通过。
