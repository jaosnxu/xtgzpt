# AUDIT-004 DEV-004 阶段自检与收口

## 状态

- 状态：执行中
- Issue：`https://github.com/jaosnxu/xtgzpt/issues/16`
- 基线：`main` after DEV-004
- 目的：进入 DEV-005 前，对 DEV-004 审计日志基础设施做统一自检、审计、修复和复测

## 审计范围

- AuditLog 类型与迁移
- 登录成功 / 失败审计
- 无权限访问审计
- 未实现动作审计
- 全局审计查询
- 对象审计查询
- 用户审计查询
- 审计查询权限与组织数据范围
- 前端 DEV-004 阶段状态
- 未开发业务边界

## 发现问题

### AUDIT-004-P2-001 DEV-004 合并后页面仍显示进行中

现象：

- DEV-004 已合并到 `main`
- 前端首页和设置模块仍显示 `DEV-004 进行中`
- 顶部阶段说明仍显示 `已进入 DEV-004：审计日志基础设施`

风险：

- 用户和 reviewer 会误以为 DEV-004 尚未完成
- 后续进入 AUDIT-004 / DEV-005 时阶段状态不清晰

处理：

- 首页和设置模块阶段改为 `DEV-004 已完成`
- 顶部说明改为 `DEV-004 已完成：等待 AUDIT-004 收口`

## 静态审计结果

- 审计查询接口不记录用户传入的对象查询参数到审计日志，避免猜测对象名进入审计
- `/modules/:module` 对未知模块继续先返回 404，不进入拒绝记录
- 授权通过但未开发的项目、文件、AI 接口继续返回 `501 not_implemented`
- 前端审计区域只展示能力边界，不展示模拟业务审计事实
- 数据库迁移沿用现有 UUID 主键体系；当前内存种子字符串 id 仍属于原型层，未来真实持久化绑定时需要单独映射

## 自动验证结果

| 检查项 | 结果 |
| --- | --- |
| `npm ci` | 通过 |
| `npm run ci` | 通过 |
| `npm audit --audit-level=low` | 0 vulnerabilities |
| `git diff --check` | 通过 |
| API 审计矩阵 | 通过 |
| Playwright 页面自检 | 通过 |

## API 自检结果

| 场景 | 结果 |
| --- | --- |
| 错误密码登录返回 401 | 通过 |
| `super/admin/owner/member` 登录成功 | 通过 |
| 普通成员查询 `/audit-logs` 返回 403 | 通过 |
| owner 调用 `/projects` 返回 `501 DEV-005` | 通过 |
| owner 未实现动作进入对象审计和用户审计 | 通过 |
| super 可查询对象审计和用户审计 | 通过 |
| admin 审计查询受组织范围限制 | 通过 |
| 猜测 `/files/secret-contract-name/download` 返回 403 | 通过 |
| 全局审计不包含 `secret-contract-name` | 通过 |

## 浏览器自检结果

- Browser 插件 DOM 验证可打开页面，但输入和 CDP 通道被工具层问题阻断
- 使用项目外临时 Playwright 完成页面验证，不把依赖写入仓库
- 管理员登录成功
- 首页显示 `DEV-004 已完成：等待 AUDIT-004 收口`
- 首页显示审计查询已接入
- 审计边界只展示能力，不展示模拟业务审计事实
- 系统设置页面显示权限策略摘要
- 截图：
  - `docs/dev-log/AUDIT-004-settings-admin.png`

## 已知工具问题

- 本机 `localhost:3001` 被无关 Next 服务占用；当前仓库验证必须使用 `127.0.0.1:3001`
- Browser 插件截图和输入通道在本轮出现 `virtual clipboard` / `paused document response` 问题
- 处理方式：页面打开和 DOM 读取仍用 Browser；登录、截图和最终页面验证使用项目外临时 Playwright
- 该问题属于本地验证工具问题，不影响产品代码、CI 或 API 行为

## 当前阶段边界

已完成：

- DEV-004 审计日志类型
- 审计日志迁移
- 关键动作审计写入
- 全局 / 对象 / 用户审计查询
- 审计查询权限控制
- 前端审计边界展示

未完成，不能假装已完成：

- 审计日志真实数据库持久化绑定
- 审计日志后台管理页面
- 真实项目、任务、知识、合同、审批业务流
- 真实文件上传、下载、预览
- 真实 AI 调用

## 下一步门禁

进入 DEV-005 前必须完成：

- 本审计修复合并
- `npm ci` + `npm run ci` 通过
- `npm audit --audit-level=low` 通过
- API 审计矩阵复测通过
- 浏览器页面状态复测通过
- 独立 reviewer gate 通过
