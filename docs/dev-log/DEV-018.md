# DEV-018 生产上线准备

## 状态

- 状态：生产上线准备文档和安全占位符模板已补齐；本地完整 gate 已通过；待 PR 环境复核
- 分支：当前 Loop 工作树
- 前置：`DEV-017` 页面状态和响应式收口
- Loop 任务：
  - `TASK-XTGZPT-DEV-018-CODEX-1`
  - `TASK-XTGZPT-DEV-018-CONTINUATION-CODEX-1`
  - `TASK-XTGZPT-DEV-018-CONTINUATION-2-CODEX-1`

## 范围

- 生产上线 runbook
- 生产配置和 GitHub Secrets / Variables 占位符
- PostgreSQL 兼容 migration 前后检查
- 数据库备份和恢复演练流程
- 日志、审计和健康检查
- production smoke test 操作规则
- 依赖和 secret 安全审计
- 回滚策略
- 运维交接和 release signoff

## 不做

- 不执行真实生产部署
- 不写入真实生产数据库
- 不提交真实 secret、API key、JWT secret、生产数据库连接串、私钥、token 或真实生产 URL
- 不新增业务功能、一级菜单、财务、采购、库存、ERP、移动端或外部通知
- 不放宽 AI 人工确认边界

## 交付物

- `docs/operations/PRODUCTION_READINESS_RUNBOOK.md`
  - 已覆盖配置、GitHub Secrets、migration、备份、恢复、日志、健康检查、smoke、安全审计、回滚、运维交接和签字。
- `.env.example`
  - 已补齐生产上线相关占位符。
  - 只包含 placeholder，不包含真实 secret。
- `docs/dev-log/DEV-018.md`
  - 本阶段执行记录。
- `docs/dev-log/AUDIT-018.md`
  - 本阶段审计记录。
- `docs/loop/runtime-memory/*`
  - Loop runtime memory 记录 DEV-018 执行过程。

## 执行记录

| 时间 | 动作 | 结果 |
| --- | --- | --- |
| 2026-06-20 | 启动 `TASK-XTGZPT-DEV-018-CODEX-1` | writer 进入执行但无业务 diff |
| 2026-06-20 | 启动 `TASK-XTGZPT-DEV-018-CONTINUATION-CODEX-1` | writer 写入 runtime memory，但未产出 DEV-018 文档 |
| 2026-06-20 | 启动 `TASK-XTGZPT-DEV-018-CONTINUATION-2-CODEX-1` | writer 重复空转，记录为 Loop executor 问题 |
| 2026-06-20 | 通过 Loop fallback/verifier 路径补齐 DEV-018 文档和占位符模板 | 已完成 |
| 2026-06-20 | 执行完整本地 gate | 已通过 |

## 验证结果

已完成：

- `git diff --check`：通过
- secret 占位符检查：未提交真实 `ARK_API_KEY`、真实 `DATABASE_URL`、真实 `JWT_SECRET`、私钥或 token
- `npm run lint`：通过
- `npm run typecheck`：通过
- `npm run test`：通过，14 个测试文件、56 个用例
- `npm run build`：通过
- `npm run smoke:api`：通过，29 个 smoke 检查点
- `npm audit --audit-level=low`：通过，0 vulnerabilities
- 范围检查：未修改产品业务行为、权限、菜单、AI 执行边界或生产环境

待 PR / verifier 环境复核：

- GitHub required checks

## 当前限制

- DEV-018 没有执行真实生产部署。
- DEV-018 没有执行真实 production smoke，因为缺少真实生产 base URL 和外部注入的 smoke 账号。
- DEV-018 没有执行真实备份或恢复，只交付 PostgreSQL 兼容操作模板和上线前演练要求。
- Loop `codex_executor` 在本阶段多次出现无业务 diff 空转，已记录为 Loop 平台问题；不计为产品交付失败。

## 下一步

- 在 PR 环境跑完整 required checks。
- 如需要真实上线，按 `docs/operations/PRODUCTION_READINESS_RUNBOOK.md` 由 release operator 在受控 release window 执行。
- 若继续完善 Loop 平台，应补充 `codex_executor` 无业务 diff / 无输出快速失败机制，避免长时间空转。
