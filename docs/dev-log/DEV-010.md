# DEV-010 角色和权限生产化

## 状态

- 状态：本地代码 gate 通过，待真实 Git worktree / 在线 npm audit 复核
- 分支：`loop/dev-010-role-permission-productionization`
- 前置：`DEV-009` 生产持久化底座
- Loop 任务：`TASK-XTGZPT-DEV-010-CODEX-2`

## 范围

- 共享模型补齐 11 类 Phase 1 角色
- 权限摘要按菜单、数据、操作、审批、文件、AI 六个维度输出
- 审批权限独立为 `approval` 维度，不再混在 `operation` 中
- 新增审批权限 helper 和审批权限策略 API 输出
- 系统管理员保持配置范围，不默认拥有全部业务数据
- 普通用户访问系统设置和权限 API 被后端阻断并写拒绝审计
- 新增审批权限策略数据库迁移资产
- 更新前端权限摘要展示和设置页计数

## 不做

- 不实现完整合同流程
- 不实现完整审批流程
- 不实现文件二进制存储
- 不做部署或生产写入
- 不处理 secrets
- 不做财务模块或 ERP 扩张
- 不修改 Loop 平台仓库

## 当前执行记录

| 时间 | 动作 | 结果 |
| --- | --- | --- |
| 2026-06-20 | 读取 DEV-010 必读项目文档 | 已完成 |
| 2026-06-20 | 扩展 `packages/shared` 角色与权限模型 | 已完成 |
| 2026-06-20 | 新增 11 类角色 seed 用户和登录凭据 | 已完成 |
| 2026-06-20 | 增加 `ApprovalPermission` 与 `canPerformApprovalAction` | 已完成 |
| 2026-06-20 | API 增加 `/settings/approval-permission-policies` | 已完成 |
| 2026-06-20 | 普通用户设置和审批权限 API 拒绝接入审计 | 已完成 |
| 2026-06-20 | 新增 `0005_approval_permission_policies.sql` | 已完成 |
| 2026-06-20 | 增加共享模型和 API 权限测试 | 已完成 |
| 2026-06-20 | 更新 backlog、技术标准、项目宪法和运行状态 | 已完成 |

## 验收标准

- 11 类角色都可登录测试
- 权限摘要输出六个独立维度：menu、data、operation、approval、file、ai
- 审批权限不再混在操作权限中
- 系统管理员不默认读取全部业务数据
- 普通用户不能访问系统设置和权限策略 API
- 无权限访问有拒绝审计记录
- 审批权限策略有数据库迁移资产
- AI 保持草稿、建议、提醒边界，不成为审批人

## 验证结果

自动验证：

- `npm ci`：通过
- `npm run typecheck`：通过
- `npm run test`：通过，8 个测试文件 / 42 个测试通过
- `npm run lint`：通过
- `npm run build`：通过
- `npm run smoke:api`：通过
- `npm run ci`：通过
- `npm audit --audit-level=low --offline`：0 vulnerabilities
- `npm audit --audit-level=low`：阻塞，当前环境无法解析 `registry.npmjs.org`
- `git diff --check`：阻塞，当前托管 worktree 无 `.git` 元数据
- 触达文件空白 / 冲突标记扫描：通过

新增测试覆盖：

- 11 类 Phase 1 seed 角色均可登录
- 权限摘要包含 menu、data、operation、approval、file、ai 六维
- 审批权限 helper 要求当前节点人类审批人
- 系统管理员可配置权限但只能看到授权组织业务数据
- 普通用户访问系统设置被 403 阻断
- 普通用户访问审批权限策略 API 被 403 阻断
- 拒绝事件可由管理员查询且不泄露猜测对象名

## 当前边界

已完成：

- DEV-010 角色和权限生产化代码边界
- 审批权限独立维度和策略输出
- 审批权限策略数据库迁移资产
- 本阶段未修改 Loop 平台仓库

未完成，不能假装已完成：

- 完整审批闭环
- 完整合同闭环
- 数据库 adapter 读取后台权限配置
- 文件生产存储
- 在线 npm audit 和 Git diff gate 受当前环境限制，PR 前必须补跑

## 下一步

- 在真实 Git worktree 执行 `git diff --check`
- 在可访问 npm registry 的环境执行在线 `npm audit --audit-level=low`
- 创建 PR 并跑 GitHub required checks
- 进入 `DEV-011` 我的工作台、通知和页面状态收口
