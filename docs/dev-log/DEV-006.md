# DEV-006 聊天与 AI 整理草稿

## 状态

- 状态：本地开发完成，待提交 PR
- 分支：`dev/006-chat-ai-drafts`
- Issue：`https://github.com/jaosnxu/xtgzpt/issues/27`
- 前置：`DEV-005` 已合并到 `main`

## 范围

- 聊天会话创建
- 聊天会话列表
- 聊天会话详情
- 聊天消息发送
- 会话成员可见性控制
- 非成员访问 404 且不泄露会话标题
- AI 会话总结草稿
- AI 任务草稿
- AI 知识草稿
- 火山方舟 / 豆包 Chat API Provider
- `.env.example` AI Provider 配置样例
- AI 草稿只保存为草稿，不自动发布知识或正式创建任务
- 聊天与 AI 动作审计记录
- `/modules/chat` 模块状态接口
- 前端聊天页面真实 API 接入
- smoke 覆盖聊天与 AI 草稿主路径

## 不做

- 不把真实 LLM API Key 提交到公开仓库
- 不做真实文件上传、下载、预览
- 不做知识库正式发布流程
- 不做 AI 自动创建正式任务
- 不做聊天搜索、已读回执、富文本和消息撤回
- 不做合同、审批、财务系统能力

## 当前执行记录

| 时间 | 动作 | 结果 |
| --- | --- | --- |
| 2026-06-19 | 创建 Issue #27 | 已完成 |
| 2026-06-19 | 创建分支 `dev/006-chat-ai-drafts` | 已完成 |
| 2026-06-19 | 增加共享 `ChatThreadRecord` / `ChatMessageRecord` / `AiDraftRecord` 类型 | 已完成 |
| 2026-06-19 | API 接入聊天会话列表、创建、详情、消息列表、消息发送 | 已完成 |
| 2026-06-19 | API 接入 AI 总结草稿、任务草稿、知识草稿 | 已完成 |
| 2026-06-19 | API 接入聊天与 AI 审计记录 | 已完成 |
| 2026-06-19 | 前端接入聊天页面真实数据 | 已完成 |
| 2026-06-19 | `/modules/chat` 状态改为 `available` | 已完成 |
| 2026-06-19 | `npm run typecheck` | 通过 |
| 2026-06-19 | `npm run test` | 通过 |
| 2026-06-19 | `npm run ci` | 通过 |
| 2026-06-19 | `npm run smoke` | 通过 |
| 2026-06-19 | `npm audit --audit-level=low` | 0 vulnerabilities |
| 2026-06-19 | `git diff --check` | 通过 |
| 2026-06-19 | Browser 聊天与 AI 草稿页面验证 | 发现问题后修复，复测通过 |
| 2026-06-19 | 接入火山方舟 / 豆包 Provider | 已完成，真实 Key 通过环境变量配置 |

## 验收标准

- 登录用户可以创建聊天会话
- 创建者默认进入会话成员
- 会话可以加入有效用户成员
- 无效成员创建会话返回 `invalid_chat_member`
- 成员可以查看会话和消息
- 非成员猜测会话详情返回 404 且不泄露会话标题
- 成员可以发送非空消息
- 空消息返回 `message_content_required`
- 没有消息时不能生成 AI 草稿，返回 `source_messages_required`
- 有消息后可以生成 AI 总结草稿、任务草稿、知识草稿
- AI 草稿必须带 `isDraft: true`
- AI 任务草稿不能直接创建正式任务
- 未配置 `ARK_API_KEY` 时使用模板草稿，测试和本地无密钥环境不打真实网络
- 配置 `ARK_API_KEY` 后通过方舟 Chat API 生成真实模型内容
- API Key 只能来自 `.env.local`、服务器环境变量或 GitHub Secrets
- 聊天与 AI 关键动作写入审计
- `/modules/chat` 返回 `available`
- 本地 `npm run ci` 通过

## 验证结果

自动验证：

- `npm run typecheck`：通过
- `npm run test`：通过，7 个测试文件 / 30 个测试通过
- `npm run ci`：通过
- `npm run smoke`：通过
- `npm audit --audit-level=low`：0 vulnerabilities
- `git diff --check`：通过

API 覆盖：

- `owner` 创建聊天会话成功
- `member` 可读取被加入会话
- `admin` 猜测会话详情返回 404
- `member` 发送消息成功
- AI 总结草稿生成成功
- AI 任务草稿生成成功
- AI 知识草稿生成成功
- Provider payload 构造测试通过
- Provider 方舟调用路径测试通过，使用 mock fetch，不泄露真实 Key
- AI 任务草稿不创建正式任务
- 审计日志包含 `chat.thread_created`、`chat.message_sent`、`chat.ai_summary_created`、`chat.ai_task_draft_created`、`chat.ai_knowledge_draft_created`
- `/modules/chat` 返回 `available`

浏览器验证：

- 使用 `http://127.0.0.1:3001`
- `owner` 登录成功
- 聊天模块可打开
- 创建聊天会话成功
- 发送消息成功
- 消息发送后 AI 总结、任务草稿、知识草稿按钮可用
- AI 总结草稿生成成功
- AI 任务草稿生成成功
- AI 知识草稿生成成功
- 三类草稿均显示 `1 来源`
- 截图：
  - `docs/dev-log/DEV-006-chat-ai-drafts.png`

## 发现与处理

### DEV-006-P2-001 AI 权限上下文不能用空参与人资源

现象：

- 初始实现中，AI 能力校验只传入组织资源，没有传入会话成员
- 普通会话成员可能被错误拒绝使用 AI 草稿能力

处理：

- AI 草稿生成时先确认用户可见会话
- 再按 `{ organizationId, participantUserIds: thread.memberUserIds }` 校验 AI 能力
- 增加测试覆盖成员生成 AI 草稿

### DEV-006-P3-001 AI 草稿必须保持草稿边界

现象：

- 协同平台后续会有正式任务和正式知识库
- 本阶段如果直接创建正式对象，会污染后续阶段边界

处理：

- AI 输出统一写入 `AiDraftRecord`
- 返回 `isDraft: true`
- 测试确认 AI 任务草稿不会进入正式任务列表

### DEV-006-P2-002 前端 AI 草稿请求空 JSON 体导致浏览器 Bad Request

现象：

- API 测试和 smoke 通过
- 真实浏览器点击 `整理摘要` / `任务草稿` / `知识草稿` 时页面显示 `Bad Request`
- 原因是前端统一发送 `Content-Type: application/json`，但 AI 草稿 POST 没有 `body`
- 浏览器请求进入 Fastify 后被当作坏 JSON 请求处理

处理：

- 前端 AI 草稿请求显式发送 `body: JSON.stringify({})`
- 重新浏览器验证通过，三类 AI 草稿均正常生成

### DEV-006-P1-003 公开仓库不能提交真实 API Key

现象：

- 项目仓库是 public
- 用户提供了火山方舟 / 豆包模型与 API Key
- 若把 Key 写入代码、README、测试、截图或提交历史，会造成泄密风险

处理：

- `.gitignore` 已排除 `.env` 和 `.env.*`
- 只提交 `.env.example`，不包含真实 Key
- API 代码只读取 `ARK_API_KEY`、`ARK_BASE_URL`、`ARK_MODEL`、`ARK_AI_MAX_TOKENS`
- 本地开发由 `.env.local` 或 shell 环境变量提供 Key
- CI / 生产环境必须使用 GitHub Secrets 或服务器 Secrets

### DEV-006-P2-004 AI Provider 需要无密钥降级和测试隔离

现象：

- 本地测试和 GitHub CI 不能依赖真实 API Key
- 有 Key 时需要走真实模型，无 Key 时系统仍应可开发、可测试

处理：

- 增加 `ai-provider`
- 未配置 `ARK_API_KEY` 时返回模板草稿，`frameworkVersion=chat-ai-template-v1`
- 配置 `ARK_API_KEY` 时调用方舟 Chat API，`frameworkVersion=ark-doubao-chat-v1`
- 测试环境默认不发真实网络请求
- Provider 测试使用 mock fetch 验证 endpoint、鉴权 Header、模型和 payload

## 当前边界

已完成：

- 聊天基础闭环
- 聊天成员可见性
- AI 总结 / 任务 / 知识草稿
- 火山方舟 / 豆包 Provider
- 无密钥模板降级
- AI 草稿审计
- 前端聊天页面
- 聊天模块状态接口

未完成，不能假装已完成：

- MCP 生产调用
- 真实生产环境 Secret 配置
- 聊天数据库持久化
- 文件附件生产存储
- 正式知识库发布
- AI 草稿转正式任务
- 聊天搜索、回执、富文本

## 下一步

- 创建 DEV-006 PR
- 等待 required checks
- 完成 reviewer gate
- 合并后创建 `AUDIT-006`
