# DOCS-001 项目宪法与生产标准收口

## 状态

- 状态：执行中
- 分支：`docs/project-constitution`
- 类型：治理文档收口

## 背景

项目已有冻结规则、开发 PRD、权限矩阵、状态机、验收标准和 Loop 规则，但缺少一个统一的项目宪法入口。

同时旧冻结规则仍保留“禁止进入开发、数据库字段、API”的原型前口径，已经不符合当前 DEV-008 之后的实际状态。

## 范围

- 新增项目宪法
- 新增技术标准
- 新增业务实现阶段计划
- 新增测试验收标准
- 更新 Loop memory index
- 更新旧冻结规则当前状态
- 更新 backlog 当前阶段状态

## 不做

- 不改业务代码
- 不改 API 行为
- 不改数据库迁移
- 不扩大业务范围
- 不把项目变成 ERP 或财务系统

## 验证

- `git diff --check`

## 验证结果

- 当前禁令检索：已确认旧口径只保留为“已解除”的历史说明，不再作为当前限制。
- `git diff --check`：通过。
