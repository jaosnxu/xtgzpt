# DEV-009 生产持久化底座

## 状态

- 状态：本地代码门禁通过，待真实 Git worktree / PR gate 复核
- 分支：`loop/dev-009-persistence-foundation`
- 前置：`DEV-008` 项目记忆检索与回用
- Loop 任务：`TASK-XTGZPT-DEV-009-CODEX-4`

## 范围

- 把 API 运行时数据从散落的进程内数组收口到统一运行时数据仓库
- 支持通过 `buildServer({ dataFilePath })` 将运行时业务数据写入文件
- 覆盖项目、任务、聊天会话、聊天消息、AI 草稿、知识、项目记忆、审计日志、拒绝访问事件
- 新增服务重启后数据仍可读取的自动测试
- 新增 PostgreSQL 生产表结构迁移草案
- 写回 Loop 项目运行记忆

## 不做

- 不直接连接生产数据库
- 不写入生产环境
- 不处理 secrets
- 不修改 Loop 平台代码
- 不开发合同、审批、文件正式上传模块
- 不改变现有 API 返回结构

## 当前执行记录

| 时间 | 动作 | 结果 |
| --- | --- | --- |
| 2026-06-19 | Loop 读取项目记忆索引与运行记忆 | 已完成 |
| 2026-06-19 | 创建分支 `loop/dev-009-persistence-foundation` | 已完成 |
| 2026-06-19 | 新增 `apps/api/src/runtime-store.ts` | 已完成 |
| 2026-06-19 | `buildServer` 接入统一运行时数据仓库 | 已完成 |
| 2026-06-19 | 写动作接入 `store.save()` | 已完成 |
| 2026-06-19 | 新增 `runtime-store.test.ts` 重启持久化测试 | 已完成 |
| 2026-06-19 | 新增 `0004_runtime_collaboration_records.sql` | 已完成 |
| 2026-06-19 | Loop 重新执行项目读取、记录、审查和验证 | 通过 |
| 2026-06-19 | Codex-4 复核并采用既有 DEV-009 运行时仓库实现 | 已完成 |
| 2026-06-19 | 扩展重启持久化测试，覆盖聊天、消息、AI 草稿、知识和项目记忆 | 已完成 |
| 2026-06-19 | 修正 DEV-009 migration 中用户 / 组织外键类型，并补状态历史与拒绝访问事件表 | 已完成 |
| 2026-06-19 | `smoke:api` 从 `tsx` CLI 切换为 `node --import tsx`，规避托管沙箱 IPC 限制 | 已完成 |
| 2026-06-19 | `npm run typecheck` | 通过 |
| 2026-06-19 | `npm run test` | 通过，34 个测试通过 |
| 2026-06-19 | `npm run lint` | 通过 |
| 2026-06-19 | `npm run build` | 通过 |
| 2026-06-19 | `npm run smoke:api` | 通过 |
| 2026-06-19 | `npm run ci` | 通过 |
| 2026-06-19 | `npm audit --audit-level=low --offline` | 0 vulnerabilities |
| 2026-06-19 | `npm audit --audit-level=low` | 阻塞：当前环境无法解析 `registry.npmjs.org` |
| 2026-06-19 | `git diff --check` | 阻塞：当前托管 worktree 无 `.git` 元数据 |
| 2026-06-19 | 触达文件空白 / 冲突标记扫描 | 通过 |

## 验收标准

- API 业务数据不再由 `buildServer` 内部散落数组直接裸露管理
- 项目和任务创建后，API 运行时重启仍能读取
- 关键审计记录重启后仍能查询
- 原有项目、任务、聊天、AI 草稿、知识检索、项目记忆 smoke 不回退
- PostgreSQL 表结构覆盖当前运行时对象
- 本阶段不污染 Loop 平台仓库

## 验证结果

自动验证：

- `npm run typecheck`：通过
- `npm run test`：通过，8 个测试文件 / 34 个测试通过
- `npm run lint`：通过
- `npm run build`：通过
- `npm run smoke:api`：通过
- `npm run ci`：通过
- `npm audit --audit-level=low --offline`：0 vulnerabilities
- `npm audit --audit-level=low`：受限网络下失败，错误为 `getaddrinfo ENOTFOUND registry.npmjs.org`
- `git diff --check`：当前目录不是 Git repository，无法执行
- 触达文件空白 / 冲突标记扫描：通过

新增测试覆盖：

- 创建项目
- 创建任务
- 关闭第一轮 API runtime
- 用同一个 `dataFilePath` 创建第二轮 API runtime
- 重新登录
- 查询项目、任务和对象审计
- 验证重启后数据仍存在
- 创建项目关联聊天会话
- 发送聊天消息
- 生成摘要、任务、知识三类 AI 草稿
- 人工确认摘要为项目记忆
- 人工确认知识草稿为正式知识条目
- 重启后读取会话、消息、AI 草稿、知识、项目记忆
- 重启后知识检索同时返回 `knowledge_item` 和 `project_memory`

## 当前边界

已完成：

- 本地可验证的运行时持久化边界
- 生产数据库表结构迁移草案，覆盖项目、任务、聊天、消息、AI 草稿、知识、项目记忆、状态历史和拒绝访问事件
- 重启不丢项目、任务、聊天、消息、AI 草稿、知识、项目记忆和审计的自动测试
- 本阶段未修改 Loop 平台仓库

未完成，不能假装已完成：

- PostgreSQL adapter
- 迁移执行器
- 生产数据库连接池
- 事务边界
- 数据备份与恢复
- 文件对象正式持久化
- 当前托管 worktree 缺少 `.git`，PR 前仍需在真实 Git worktree 执行 `git diff --check`
- 当前环境网络受限，PR 前仍需在可访问 npm registry 的环境执行在线 `npm audit --audit-level=low`

## 下一步

- 在真实 Git worktree 执行 `git diff --check`
- 在可访问 npm registry 的环境执行在线 `npm audit --audit-level=low`
- 创建 PR 并跑 GitHub required checks
- 后续阶段补 PostgreSQL adapter 与迁移执行器
